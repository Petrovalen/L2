# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Результат калибровки — машинно/разрешение-зависимый, генерируется утилитой
l2bot/calibration_result.txt
# Пользовательские настройки панели (профили ПК/персонажей, калибровки) —
# машинно-зависимые, у каждого ПК свои.
l2bot/settings.json
# Эталоны цифр для быстрого распознавания — под конкретный шрифт/разрешение,
# самообучаются в рантайме.
l2bot/digit_templates.json
# Белый список мобов (mobs.json) и снимки-шаблоны ников (target_templates/)
# НЕ игнорируются намеренно — они общие для всех персонажей и компьютеров и
# синхронизируются через git.

# Локальный бэкап-патч сессии (не для репозитория)
_session_backup/

# ОС
.DS_Store
Thumbs.db
